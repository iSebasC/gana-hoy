---
import { Button } from "@/components/ui/button";
---
<section class="relative bg-blue-900 text-white py-16 px-8 md:px-16">
  <!-- Contenido principal -->
  <div class="lg:w-1/2 space-y-6 text-center lg:text-left">
    <h1 class="text-4xl md:text-5xl font-extrabold leading-tight">
      Ahorra de forma <span class="text-orange-500">segura</span> y <br />
      automatizada con <span class="text-orange-500">GanaHoy</span>
    </h1>
    <p class="text-lg md:text-xl text-gray-200">
      Comienza a alcanzar tus metas financieras con planes que <br />
      se adaptan a tus necesidades.
    </p>
    <div class="space-x-4">
      <Button className="bg-orange-500 hover:bg-orange-600 text-white">
        ¡Empieza ahora!
      </Button>
      <Button variant="outline" className="text-white border-white hover:bg-white hover:text-blue-900">
        Ver más
      </Button>
    </div>
  </div>

  <!-- Imagen o Ilustración -->
  <div class="relative mt-10 lg:mt-0 lg:absolute lg:bottom-[-150px] lg:right-[50px]">
    <!-- Gradiente Decorativo -->
    <div class="hidden lg:block absolute bottom-[-150px] right-[50px] w-[300px] h-[300px] md:w-[400px] md:h-[400px] bg-gradient-to-r from-orange-500 via-red-500 to-pink-500 rounded-full blur-3xl opacity-30"></div>

    <!-- Contenedor de la Imagen -->
    <div class="mx-auto lg:absolute lg:bottom-[-250px] lg:right-[50px] z-10 w-[250px] md:w-[300px] lg:w-[300px] bg-gray-100 rounded-xl shadow-lg border border-gray-200 p-4">
      <img
        src="/app_gana_hoy.png" 
        alt="App Screenshot"
        class="w-full h-auto rounded-lg"
      />
    </div>
  </div>
</section>

<div class="md:h-[300px]"></div>

